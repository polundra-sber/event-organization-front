export type User = {
  login: string;
  email: string;
  name: string;
  surname: string;
  role?: string;
};

export type UserDemo = {
  login: string;
  email: string;
  name: string;
  surname: string;
};
